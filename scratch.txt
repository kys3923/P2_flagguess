sequelize model:create --name user --attributes email:string,name:string,password:string
sequelize model:create --name score --attributes userId:integer,q10score:integer,endscore:integer,streak:integer
sequelize model:create --name comment --attributes name:string,userId:integer,comment:text
sequelize model:create --name scoreboard --attributes name:string,userId:integer,q10score:integer,endscore:integer



npm i dotenv ejs express express-ejs-layouts morgan pg sequelize


<% game.questionArray.push({restCountry}); %>
<% console.log(questionArray) %>

<% function randomFlag(questionArray) { %>
  <%  let m = questionArray.length, t, i; %>
   <% while (i < m) { %>
    <%    i = Math.floor(Math.random() * m--); %>
    <%    t = questionArray[m]; %>
    <%    questionArray[m] = questionArray[i]; %>
    <%    questionArray[i] = t; %>
    <% } %>
    <% return questionArray; %>
<% } %>
<% console.log(randomFlag(questionArray), "this is shuffled array"); %>

<% function pickedCountry() { %>
    <% let correctAnswer = Math.floor(Math.random() * questionArray.length) %>
    <% let i = correctAnswer; %>
    <% console.log(questionArray[i], "this is picked one of shuffled array"); %>
<% } %>